@model IEnumerable<CTS_System6.Models.TranslatorsLanguages>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Id)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TranslatorId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.FromLanguage)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ToLanguage)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TranslatorId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FromLanguage)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ToLanguage)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.TranslatorId }) |
                @Html.ActionLink("Details", "Details", new { id=item.TranslatorId }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.TranslatorId })
            </td>
        </tr>
}
    </tbody>
</table>
